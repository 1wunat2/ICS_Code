/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package edu.hdsb.gwss.ics4u.u7;

import java.io.FileNotFoundException;
import java.io.IOException;

/**
 *
 * @author nwu10
 */
public class RecordWriter {

    /**
     * @param args the command line arguments
     * @throws java.io.FileNotFoundException
     */
    public static void main(String[] args) throws FileNotFoundException, IOException {
        /*
        System.out.println("Creating 4 Class Records");
        Record c1 = new Record("James", "Darkness", 2003, 1, true);
        Record c2 = new Record("Nathan", "My", 1999, 2, false);
        Record c3 = new Record("Isaac", "Old", 2004, 1, false);
        Record c4 = new Record("Muir", "Friend", 2003, 1, true);
        System.out.println("*********************");

        // RANDOM ACCESS FILE
        RandomAccessFile raf = new RandomAccessFile("book_info.dat", "rw");

        raf.seek(raf.length());

        raf.writeChars(c1.getAuthor());
        raf.writeChars(c1.getTitle());
        raf.writeInt(c1.getPublishYear());
        raf.writeInt(c1.getVolume());

        raf.writeChars(c2.getAuthor());
        raf.writeChars(c2.getTitle());
        raf.writeInt(c2.getPublishYear());
        raf.writeInt(c2.getVolume());

        raf.writeChars(c3.getAuthor());
        raf.writeChars(c3.getTitle());
        raf.writeInt(c3.getPublishYear());
        raf.writeInt(c3.getVolume());

        raf.writeChars(c4.getAuthor());
        raf.writeChars(c4.getTitle());
        raf.writeInt(c4.getPublishYear());
        raf.writeInt(c4.getVolume());
         */
    }
}
